export const environment = {
  production: false,
  API: "http://localhost:58448/api/",
  version: "versão 7.1",
};

// Para rodar localmente, utilizar o comando >> ng serve --configuration-qa
// Para buildar localmente, utilizar o comando >> ng build --configuration=qa