export const environment = {
  production: true,
  API: "http://localhost:58448/api/",
  version: "versão 7.3",
};

// Para rodar localmente, utilizar o comando >> ng serve --prod
// Para buildar localmente, utilizar o comando >> ng build --prod