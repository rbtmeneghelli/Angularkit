export const environment = {
  production: false,
  API: "http://localhost:58448/api/",
  version: "versão 7.2",
};

// Para rodar localmente, utilizar o comando >> ng serve --configuration-hml
// Para buildar localmente, utilizar o comando >> ng build --configuration=hml